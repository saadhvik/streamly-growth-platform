# Runtime dependencies for deploying app/streamlit_app.py.
#
# Streamlit Community Cloud installs from this file. It mirrors the runtime
# dependencies in pyproject.toml -- dev tooling (pytest, ruff, mypy) is
# deliberately excluded, since a deployment does not lint or test itself.
#
# Floors, not pins: these match pyproject.toml so the two cannot disagree about
# what the app needs. numpy>=2.0 is load-bearing -- sequential.py uses
# np.trapezoid, which replaced np.trapz in numpy 2.0.
duckdb>=1.0
numpy>=2.0
pandas>=2.0
scipy>=1.10
statsmodels>=0.14
streamlit>=1.30
altair>=5.0
